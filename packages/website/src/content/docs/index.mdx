---
title: Overview
description: One daemon to manage them all. The optimization layer for your MCP infrastructure.
---

muxed is the optimization layer for your MCP infrastructure. It runs as a background daemon that aggregates Model Context Protocol servers, keeping tool schemas out of your model's context window so agents run faster, pick the right tool, and give better results.
